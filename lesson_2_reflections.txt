What happens when you initialize a repository?  Why do you need to do it?
	When a repository is initialized, tracking is initialized.  This is 
	a necessary step for version control.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
	The staging area sits between the working directory and the repository.
	It serves as a layer to retain files so that commits can be more selective.



